"use client";
import ScrollReveal from "./ScrollReveal";

const team = [
  { name: "[NOM_GRAPHISTE]", role: "Graphiste", photo: "/team/graphiste.jpg" },
  { name: "[NOM_MONTEUR_1]", role: "Monteur Vidéo", photo: "/team/monteur1.jpg" },
  { name: "[NOM_MONTEUR_2]", role: "Monteur Vidéo", photo: "/team/monteur2.jpg" },
  { name: "[NOM_MANAGER]", role: "Manager & Créateur de contenu", photo: "/team/manager.jpg" },
];

export default function Team() {
  return (
    <section id="equipe" className="py-20 px-6 max-w-6xl mx-auto">
      <h2 className="text-3xl md:text-4xl font-heading font-bold text-center mb-16">L’équipe</h2>
      <div className="grid sm:grid-cols-2 md:grid-cols-4 gap-8">
        {team.map((member, i) => (
          <ScrollReveal key={i}>
            <div className="text-center">
              <div className="w-40 h-40 mx-auto rounded-full overflow-hidden border-2 border-geo-yellow mb-4">
                <img src={member.photo} alt={member.name} className="w-full h-full object-cover" />
              </div>
              <h3 className="text-lg font-heading font-bold">{member.name}</h3>
              <p className="text-geo-yellow text-sm">{member.role}</p>
            </div>
          </ScrollReveal>
        ))}
      </div>
    </section>
  );
}