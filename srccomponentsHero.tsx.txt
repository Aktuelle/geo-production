"use client";
import { motion } from "framer-motion";

export default function Hero() {
  return (
    <section className="relative h-screen flex items-center justify-center overflow-hidden">
      {/* Vidéo en fond */}
      <video
        autoPlay
        muted
        loop
        playsInline
        className="absolute inset-0 w-full h-full object-cover"
      >
        <source src="/videos/showreel.webm" type="video/webm" />
        {/* Fallback image si besoin */}
      </video>
      <div className="absolute inset-0 bg-geo-black/60" /> {/* Overlay sombre */}

      <div className="relative z-10 text-center px-4 max-w-3xl">
        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="text-4xl md:text-6xl font-heading font-extrabold uppercase mb-6"
        >
          [VOTRE SLOGAN PERCUTANT]
        </motion.h1>
        <motion.a
          href="#contact"
          className="inline-block bg-geo-yellow text-geo-black font-heading font-bold px-8 py-4 rounded-full hover:scale-105 transition-transform"
          whileHover={{ scale: 1.1 }}
          whileTap={{ scale: 0.95 }}
        >
          Lancer un projet
        </motion.a>
      </div>
    </section>
  );
}